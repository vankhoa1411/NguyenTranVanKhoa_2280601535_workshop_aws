---
title : "Frontend Dashboard"
date : 2026-06-10
weight : 9
chapter : false
pre : " <b> 5.9. </b> "
---

#### Build Frontend Dashboard

In this section, we will build the **Frontend Dashboard** for the **DocuMind AI** system.

The Frontend Dashboard is the primary user interface enabling users to interact with the system. Users can log in, upload documents, track document processing progress, view OCR extraction results, inspect AI Analysis summaries, chat with documents, and experiment with advanced features in the Enterprise Sandbox.

Once backend features such as the upload API, SQS Worker, Textract OCR, AI Gateway, and PostgreSQL + Prisma database are established, the frontend serves to display the entire document processing pipeline in a visual, user-friendly format.

---

#### The Role of Frontend in DocuMind AI

In the **DocuMind AI** project, the frontend is responsible for:

* Rendering the Home Page describing the system.
* Enabling user registration and login flows.
* Displaying user authentication state, avatars, and profile details.
* Supporting PDF, PNG, JPG, and JPEG document uploads.
* Listing all uploaded documents.
* Monitoring document processing status.
* Displaying OCR Result text extracted via Amazon Textract.
* Displaying AI Analysis summaries generated by Gemini/OpenAI.
* Supporting AI Chat/RAG interactive questions.
* Providing the Enterprise Sandbox to test semantic search, entities, and analytics.
* Enforcing conditional UI rendering based on User and Admin roles.

---

#### Frontend Dashboard Architecture

Overall integration flow:

```text
React Frontend
  |
  v
Backend API Node.js/Express
  |
  |-- Auth API
  |-- Document Upload API
  |-- Document Status API
  |-- OCR Result API
  |-- AI Analysis API
  |-- AI Chat/RAG API
  |
  v
PostgreSQL + Prisma
  |
  v
S3 / SQS / Textract / Gemini / OpenAI
```

![frontend-dashboard-flow](/images/5-Workshop/5.9-Frontend-Dashboard/frontend-dashboard-flow.png)

> ⚠️ **Screenshot Suggestion:**
> Draw a diagram illustrating the React Frontend connecting to the Backend API, which then interacts with PostgreSQL, Amazon S3, Amazon SQS, Amazon Textract, the Gemini API, and the OpenAI ChatGPT API.
> Save the image at the path:
> `/static/images/5-Workshop/5.9-Frontend-Dashboard/frontend-dashboard-flow.png`

---

#### Core UI Screen Layouts

The DocuMind AI Frontend Dashboard consists of the following core screens:

| Screen             | Purpose                                           |
| ------------------ | ------------------------------------------------- |
| Home Page          | Welcome page introducing DocuMind AI              |
| Login/Register     | Authenticates user credentials                    |
| Document Dashboard | Lists and manages uploaded documents              |
| Document Detail    | Displays OCR Results and AI Analysis summaries    |
| AI Chat/RAG        | Interactive context-aware Q&A with documents      |
| Enterprise Sandbox | Trial area for semantic search, entities, and analytics |
| Admin Panel        | Administrator system-wide management features      |

---

#### Detailed Content

In section **5.9 - Frontend Dashboard**, we will perform the following steps:

* [Home Page and User Authentication](5.9.1-home-and-auth/)
* [Document Dashboard](5.9.2-document-dashboard/)
* [AI Chat and RAG](5.9.3-ai-chat-rag/)
* [Enterprise Sandbox](5.9.4-enterprise-sandbox/)

---

#### Frontend Technology Stack

The DocuMind AI frontend utilizes the following technologies:

```text
React
TypeScript
TailwindCSS
Framer Motion
Axios
React Router
Context API or Zustand
```

Stack descriptions:

| Technology      | Description                        |
| --------------- | ---------------------------------- |
| React           | UI component rendering library     |
| TypeScript      | Typing support for static safety   |
| TailwindCSS     | Rapid utility-first styling        |
| Framer Motion   | Interactive visual animations      |
| Axios           | HTTP request client to backend APIs|
| React Router    | Client-side layout routing         |
| Context/Zustand | Auth and global state management   |

---

#### Recommended UI Style Guidelines

The frontend should maintain a clean, modern design language suitable for an AI SaaS platform:

* Soft light-blue or sky background tones.
* Glassmorphism layout cards.
* Pronounced border radius.
* Subtle shadow offsets.
* Lightweight micro-interactions.
* Identifiable SVG iconography.
* Structured, highly legible dashboard tables.
* Responsive compatibility for desktop, tablet, and mobile breakpoints.
* Avoidance of conflicting color palettes.

Primary color definitions:

```text
Blue
Sky
White
Slate
Emerald for success
Red for error
Amber for processing
```

---

#### User Navigation Flow

Primary user action pipeline:

```text
User visits Home Page
  |
  v
Register / Login
  |
  v
Access Document Dashboard
  |
  v
Upload document
  |
  v
Monitor processing status
  |
  v
Inspect OCR Result and AI Analysis
  |
  v
Chat with document
  |
  v
Explore Enterprise Sandbox features
```

---

#### Displaying Document Statuses on the Dashboard

The frontend must visually represent document processing stages:

```text
PENDING
UPLOADED
QUEUED
PROCESSING
OCR_COMPLETED
AI_ANALYZING
COMPLETED
FAILED
```

Progress tracking definitions:

| Status          | Progress | Description          |
| --------------- | -------: | -------------------- |
| `PENDING`       |        5 | Initial metadata set |
| `UPLOADED`      |       15 | File uploaded to S3  |
| `QUEUED`        |       25 | Waiting for worker   |
| `PROCESSING`    |       45 | OCR extraction running|
| `OCR_COMPLETED` |       60 | OCR finished         |
| `AI_ANALYZING`  |       75 | AI analysis running  |
| `COMPLETED`     |      100 | Completed            |
| `FAILED`        |        0 | Processing error     |

---

#### Backend APIs Consumed

Core APIs called by the frontend:

| API                                    | Purpose                                      |
| -------------------------------------- | -------------------------------------------- |
| `POST /api/auth/register`              | User registration                            |
| `POST /api/auth/login`                 | User authentication                          |
| `GET /api/auth/me`                     | Fetches authenticated profile details        |
| `POST /api/auth/logout`                | Session destruction                          |
| `POST /api/documents/upload`           | Uploads document payload                     |
| `GET /api/documents`                   | Lists user-owned documents                   |
| `GET /api/documents/:id`               | Fetches details of a specific document       |
| `GET /api/documents/:id/status`        | Queries document pipeline stage              |
| `GET /api/documents/:id/ocr`           | Fetches document OCR text                    |
| `GET /api/documents/:id/analysis`      | Fetches document AI analysis summaries       |
| `POST /api/ai/chat`                    | Submits context-aware Q&A queries            |
| `POST /api/enterprise/semantic-search` | Performs semantic search queries in Sandbox  |

---

#### Authentication State Management

The frontend tracks the following user parameters:

```text
user
token
isAuthenticated
role
loading
```

Upon a successful login session:

* Store the session token.
* Call the `/api/auth/me` profile endpoint.
* Update avatar and name displays.
* Hide Login/Register options.
* Enable dashboard navigation.
* Render the Admin Panel menu option if the role matches `ADMIN`.

---

#### Document Dashboard State

The dashboard tracks the following document parameters:

```text
documents
selectedDocument
uploading
processingStatus
loading
error
```

Upon a successful upload:

* Append the new document object to the list.
* Initialize status state to `QUEUED`.
* Initiate background polling queries.
* Trigger a successful upload notification.

---

#### AI Chat/RAG State

The AI Chat UI tracks:

```text
messages
question
provider
loading
sources
confidence
```

Upon submitting a prompt:

* Send the `documentId`, `question`, and `provider` to the backend.
* The backend retrieves the document OCR text context.
* The AI Gateway routes the request to Gemini or OpenAI.
* The frontend displays the returned answer.
* The session history is logged in the database.

---

#### Enterprise Sandbox Component

The Enterprise Sandbox enables trials of advanced features:

* Provider configuration toggles (OpenAI vs. Gemini).
* Advanced AI Chat/RAG capabilities.
* Semantic Search simulations.
* Automatic classification demonstrations.
* Entity extraction visualizers.
* Analytics export options.

This workshop does not utilize Amazon Bedrock. Ensure all labels use general terms:

```text
AI Embeddings Engine
Multi-Model AI Intelligence Core
DocuMind Multi-AI Compliance Active
OpenAI/Gemini Embedding Models
```

Do not reference:

```text
AWS Bedrock
Amazon Titan
Bedrock Embeddings
```

---

#### Client-side Security Safeguards

Ensure the following frontend constraints are maintained:

* Do not expose Gemini/OpenAI API keys on the client-side.
* Do not expose AWS secret credentials.
* Only pass JWT tokens in request header authorization blocks.
* Restrict page routing for documents belonging to other users.
* Implement login protected routes for dashboard views.
* Validate the `ADMIN` role state before rendering admin paths.
* Obfuscate verbose system errors from end-users.

---

#### Loading, Empty, and Error States

The frontend should have clear UI placeholders:

| State        | Description                               |
| ------------ | ----------------------------------------- |
| Loading      | Retrieving database details               |
| Empty        | No documents available                    |
| Uploading    | Transmitting document payload             |
| Processing   | Document is currently processing          |
| Completed    | Document finished processing              |
| Failed       | Document processing failure               |
| Unauthorized | User is not logged in                     |
| Forbidden    | Insufficient access rights                |

Example empty state text:

```text
No documents yet.
Upload your first document to start extracting and analyzing content with AI.
```

---

#### End-to-End Verification Steps

Manual verification steps:

1. Open the Home Page.
2. Register a new user or log in.
3. Verify avatar image and name display correctly.
4. Navigate to the Document Dashboard.
5. Upload a sample PDF or image.
6. Verify the document is added to the document table list.
7. Verify that status shifts from `QUEUED` to `PROCESSING`.
8. Execute the worker process to handle OCR and AI Analysis.
9. Verify that status shifts to `COMPLETED`.
10. Open the Document Detail screen.
11. Inspect the OCR Result and AI Analysis displays.
12. Launch the AI Chat/RAG tab.
13. Submit a document-specific question.
14. Navigate to the Enterprise Sandbox.
15. Perform a logout request and verify protected routes redirect to login.

---

#### Common Troubleshoot Scenarios

| Error                                | Cause                                    | Solution                                  |
| ------------------------------------ | ---------------------------------------- | ----------------------------------------- |
| Login succeeds but UI does not update| Auth state not updating                  | Validate AuthContext or Zustand variables |
| S3 uploads fail with CORS error      | Incorrect backend or S3 CORS setup       | Check bucket CORS rules                   |
| Dashboard status fails to refresh    | Polling inactive or worker stopped       | Check Document Status API                 |
| AI Analysis results are empty        | Document not in `COMPLETED` state        | Verify worker and AI Gateway status       |
| 401 Unauthorized                     | Expired session token                    | Re-authenticate the user session          |
| 403 Forbidden                        | Attempting to access other user's files  | Check backend query filters               |
| Broken layouts on mobile views       | Mobile layout not optimized              | Verify Tailwind breakpoint utilities      |
| Bedrock reference text found         | Hardcoded legacy text                    | Replace Bedrock references in code        |

---

#### Expected Outcomes

Upon completing this section, the frontend should achieve:

* The Home Page is correctly rendered.
* Login/Register features operate.
* Auth states are persisted and reloaded correctly.
* The Document Dashboard displays the list of documents.
* Document upload flows function.
* Processing statuses are updated visually.
* Document Detail screens render OCR and AI summaries.
* AI Chat/RAG functions successfully.
* Enterprise Sandbox acts as an OpenAI/Gemini trial area.
* Bedrock/Titan terminology is removed from the codebase.
* The frontend layout is fully responsive.

---

#### Expected Outcome

Following this section, DocuMind AI has a complete frontend dashboard. Users can register, upload files, monitor processing stages, view OCR and AI results, chat with their documents, and explore sandbox features.
